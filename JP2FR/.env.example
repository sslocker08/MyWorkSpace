# Stripe secret key (server-side only, never expose to the client)
STRIPE_SECRET_KEY=

# Stripe webhook signing secret (used to verify incoming webhook events)
STRIPE_WEBHOOK_SECRET=

# Public site URL, used for building absolute links (checkout success/cancel, canonical, etc.)
PUBLIC_SITE_URL=http://localhost:4321
