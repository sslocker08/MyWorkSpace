# 未解決の法務・運用上の論点（クライアント確認待ち）

このMVP実装（Stripe Checkout連携・`src/pages/api/checkout.ts` 他）は、以下の論点が
未確定のままでも動く形にしてある（例: `automatic_tax.enabled: false` 固定、配送先を
FRのみに制限）。ただし本番運用前には、下表の各項目をオーナーが確認・決定する必要がある。

| # | 論点 | 詳細 | 想定オーナー | 現状のMVP実装での扱い | ステータス |
|---|------|------|------|------|------|
| 1 | VAT / IOSS 登録 | フランス（EU）向け越境ECとして、付加価値税(VAT)登録・IOSS（Import One-Stop Shop）登録が必要か。税理士・弁護士マター。 | 税理士/弁護士 | `automatic_tax.enabled: false` で固定し、Stripe Tax による自動計算は無効化。登録方針が決まってから有効化する（`src/pages/api/checkout.ts` 内のTODOコメント参照）。 | 未着手 |
| 2 | 配送条件（DDP/DDU）と返品送料負担 | 関税・輸入消費税をどちらが負担するか（DDP=売主負担 / DDU=買主負担）。EU消費者法上の14日間クーリングオフ（無条件返品権）における返送料の負担者。 | 経営/法務 | 配送先を `shipping_address_collection.allowed_countries: ['FR']` のみに制限。DDP/DDU・返送料負担は未実装（決済画面上の明示なし）。 | 未着手 |
| 3 | 一点物（unique item）の競合注文ウィンドウ | 在庫1点物の商品に対し、Webhook反映（売り切れマーキング）が完了する前に複数注文が同時に入った場合の返金ポリシー。 | 経営 | `stock.kind === 'unique'` は数量>1のみサーバー側で拒否。複数人がほぼ同時に同一商品を購入する競合ウィンドウそのものの防止・返金フローは未実装（`src/pages/api/stripe-webhook.ts` の TODO: 売り切れマーキング機能と合わせて要設計）。 | 未着手 |
| 4 | 3言語（FR/EN/JA）翻訳の担当・品質保証 | 商品名・説明・法務文言（利用規約・返品ポリィ等）の翻訳を誰が行うか。ネイティブチェックの要否。 | コンテンツ/翻訳担当 | 商品コンテンツJSONは3言語分のフィールドを持つ構造のみ用意。翻訳の正確性・法務文言の翻訳品質は未確認。 | 未着手 |
| 5 | フォントの商用ライセンス | `DESIGN.md` で本番は自己ホストのサブセット済みWebフォントへ差し替え予定（現状はシステムフォントスタック）。商用サイトでの再配布・サブセット化がライセンス上許可されるかの確認。 | デザイン/法務 | MVPはシステムフォントスタックで自己完結（外部CDN不使用）。本番フォント差し替えは保留。 | 未着手 |
| 6 | 注文発生時の作家（founder）への通知 | 注文成立時、対象商品の作家本人へどう通知するか（メール/Slack等）・どの情報を渡すか。 | 経営/オペレーション | `checkout.session.completed` Webhookでの構造化ログ出力のみ実装。実際の通知配送は `src/pages/api/stripe-webhook.ts` 内にTODOとして残置（post-MVP）。 | 未着手 |

## 運用メモ

- 上記が未決定の間は、`automatic_tax` を有効化しない・VAT込み表記を主張しない・配送先をFRに限定する、の3点を守ることでリスクを抑制している。
- 各論点が決着したら、該当する実装ファイル（`src/pages/api/checkout.ts`, `src/pages/api/stripe-webhook.ts`）のTODOコメントを解消し、このファイルのステータス列を更新すること。
