{
  "name": "@mwstudio/root",
  "private": true,
  "version": "0.0.0",
  "description": "MyWorkSpace Studio — niche-product factory monorepo (6 reusable engines + shared platform).",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=20"
  }
}
